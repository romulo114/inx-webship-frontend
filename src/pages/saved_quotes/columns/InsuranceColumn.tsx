import React from "react";

interface PropTypes {
    cellContext: any,
}

export const InsuranceColumn = ({cellContext}: PropTypes) => {
    // const QuoteId = cellContext.row.original.quoteItem.providerQuoteId
    return (
        <div className="flex justify-center">
            can't find
        </div>
    );
};